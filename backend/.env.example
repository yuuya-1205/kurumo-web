# このファイルをコピーして .env を作り、値を埋めて使う。
#   cp .env.example .env
# .env は .gitignore 済み（パスワードをコミットしないこと）。

PORT=8080

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=kurumo_api

# silent / error / warn / info
# info にすると発行された SQL がすべてログに出る。
DB_LOG_LEVEL=warn
